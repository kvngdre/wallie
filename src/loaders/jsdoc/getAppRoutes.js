import { Router } from 'express';

/**
 * A function that implements the app routes and returns the express router.
 * @typedef {function(): Router} getAppRoutes
 * @requires module:express.Router
 */
